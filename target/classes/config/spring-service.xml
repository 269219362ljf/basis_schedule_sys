<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!--spring 线程池(可选) -->
    <bean id="taskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <!-- 核心线程数  -->
        <property name="corePoolSize" value="10" />
        <!-- 最大线程数 -->
        <property name="maxPoolSize" value="50" />
        <!-- 队列最大长度 >=mainExecutor.maxSize -->
        <property name="queueCapacity" value="1000" />
        <!-- 线程池维护线程所允许的空闲时间 -->
        <property name="keepAliveSeconds" value="300" />
    </bean>


    <!-- 扫描service包下所有标注@Service的服务组件 -->

    <!-- 扫描通用服务组件 -->
    <context:component-scan base-package="common.service" />

    <!-- 扫描调度系统的服务组件 -->
    <context:component-scan base-package="basisSchedule.scheduleService" />

    <!-- 扫描股票分析系统的服务组件 -->
    <context:component-scan base-package="stockSystem.stockService" />

    <!-- 扫描天池客流量预测的服务组件 -->
    <context:component-scan base-package="tianchiPredictCustom.tianchiPredictService" />

</beans>